Đặt file mô hình 3D của bạn (ví dụ `custom_bridge.glb`) vào thư mục này,
sau đó cập nhật đường dẫn trong `components/Physics/Bridge3D/BridgeModel.tsx`
nếu muốn đổi tên khác. File phải là định dạng GLB/GLTF để có thể được nạp
bởi react-three-fiber.

